<% content_for :title, "Markets" %>

<h1 class="text-center mb-5"> 🎄 Christmas Markets 🎄 </h1>

<div class="container" id="items">
  <div class="row row-cols-lg-4 g-5">
    <% @markets.each do |market| %>
    <div class="col">
      <div class="card mb-2">

  <%= image_tag market.image_url, class: "card-img-top", alt: market.name, style: "height: 300px; width: 100%;" %>


        <div class="card-body">
          <h5 class="card-title"><%= market.name %></h5>
        </div>

        <div class="card-footer d-flex justify-content-between">
          <small class="text-muted">In stock · €<%= market.price %></small>
          <%= link_to "Show detail", market%>
        </div>
      </div>
      </div>
    <% end %>
  </div>